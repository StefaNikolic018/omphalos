import React, { memo } from 'react';

const index = () => {
  return (
    <div className='w-[35%] h-full z-20 p-2 border-2 dark:border-[#ffffffdb] border-zinc-700 rounded-xl bg-[#ffffff72] dark:bg-zinc-900  flex flex-col flex-wrap'>
      index
    </div>
  );
};

export default memo(index);
